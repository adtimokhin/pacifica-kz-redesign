---
const {placeholder, inputName, inputType} = Astro.props;
---

<input
type={inputType}
id={inputName}
class="w-full border-2 border-black rounded-full p-4 text-gray-700 focus:outline-none focus:border-black/50"
placeholder={placeholder}
/>